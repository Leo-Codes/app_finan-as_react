import styled from "styled-components";

export const Container = styled.div`
    max-width: 95vw;
    width: 80%;
    margin: 0 auto;
    display:flex;
    /* flex-direction:row; */
    gap: 15px;
    margin-top: -50px;
    justify-content:space-around;

    font-family:'Poppins';

`;